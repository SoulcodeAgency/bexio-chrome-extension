/**
 * Kept out of the component file because `react-refresh/only-export-components` rejects
 * non-component exports there, and the tests need to address the button by its label.
 */
export const REFRESH_TEMPLATES_LABEL = "Reload templates";

export const REFRESH_TEMPLATES_TOOLTIP =
  "Re-read the templates from storage. Use this after saving a new template on the bexio page — " +
  "then 'Auto map templates' can match it.";
