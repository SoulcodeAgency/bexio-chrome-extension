unpacked
dist
**/node_modules/**
test-results
playwright-report
coverage
*.tsbuildinfo

# Claude Code: personal, per-machine state. `.claude/settings.json` IS checked in
# (see .claude/settings.json) — only these are local.
.claude/settings.local.json
.claude/worktrees/

# graft (MCP code-index server) writes a per-repo cache here. It is local,
# per-machine state — and its session files record the prompts verbatim, which
# have no business in a public repository.
graft/
